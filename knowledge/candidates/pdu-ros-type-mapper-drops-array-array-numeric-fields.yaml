id: pdu-ros-type-mapper-drops-array-array-numeric-fields
title: hakoniwa-pdu-ros silently drops rclpy array.array sequence fields, and its own tests cannot catch it because they use fake message classes
status: candidate

knowledge_type:
  - known_pitfall
  - validation_rule

summary: >
  hakoniwa-pdu-ros's type_mapper copies a ROS message field into a generated PDU type by
  branching on `isinstance(src_value, (list, tuple))`. Real rclpy messages store
  `sequence<double>` and similar primitive sequences as `array.array`, which is neither a
  list nor a tuple, so the value falls through to the struct-field branch, recurses into an
  object that has no fields, and produces nothing. No exception, no log, no warning. A
  `sensor_msgs/JointState` therefore crosses the bridge with `name` intact
  (`sequence<string>` IS a plain list in rclpy) and `position`, `velocity` and `effort`
  empty. The partial survival is what makes it hard to spot. The repository's own
  `test/test_type_mapper.py` injects fake ROS message classes whose fields are plain
  Python lists, so the array.array path is never exercised, which means the README's
  "Verified" list of PointCloud2 / JointState / LaserScan / CameraInfo /
  Float64MultiArray is verified only against those fakes. The fix measured here is one
  line plus an import.

observation:
  context: >
    Building the first Tobas (ROS 2 Jazzy) to Hakoniwa PDU bridge prototype named in
    hakoniwa-business-pack's Tobas catalog entry, on WSL2 Ubuntu 24.04 with
    RMW_IMPLEMENTATION=rmw_cyclonedds_cpp, bridging `sensor_msgs/JointState` from Tobas to
    a Hakoniwa PDU channel and decoding it on a native Windows 11 peer, 2026-07-30/31.
  details:
    - "Symptom. Passing a real rclpy sensor_msgs/JointState through the bridge produced
      `bytes=448 name=['rotor_0_joint', 'rotor_1_joint'] position=[] velocity=[] effort=[]`.
      The payload was the right size and the string array survived; only the numeric arrays
      were empty, and nothing was logged."
    - "Mechanism. hakoniwa_pdu_ros/type_mapper.py (line 77 before the patch) reads
      `elif isinstance(src_value, (list, tuple)): setattr(dst, name, _copy_list(...))`.
      Measured on this machine, `JointState.position` is an `array.array('d', [...])`,
      so the branch is skipped and the trailing `else` treats the value as a struct and
      calls _copy_matching_fields on it. array.array exposes no message fields, so the
      recursion completes having copied nothing."
    - "Why only the numbers vanish. `JointState.name` is `sequence<string>`, which rclpy
      keeps as a plain Python list, so it takes the list branch and arrives correctly. Any
      reader who checks that names came through concludes the bridge works."
    - "Blast radius, measured field by field. array.array representations that were dropped
      before the patch and correct after it: JointState.position ([] -> [0.1, 0.2]),
      LaserScan.ranges ([] -> [1.0, 2.0, 3.0]), Float64MultiArray.data
      ([] -> [1.5, 2.5, 3.5]). JointState.name (a list) was correct both times. Stated
      precisely, because the distinction matters: three fields across three of the five
      README Verified types were measured. PointCloud2 and CameraInfo were NOT measured. The
      mechanism is field-representation-dependent rather than type-dependent, so any field
      rclpy backs with array.array is expected to behave the same way, but that expectation
      is reasoning, not measurement, and the remaining types should be tested before the
      breadth is asserted."
    - "Why upstream CI is blind to it. test/test_type_mapper.py calls
      _install_fake_ros_modules() and asserts against fake message classes whose sequence
      fields are plain lists. The array.array path never runs, so the suite can pass while
      the component drops all numeric data against real rclpy."
    - "Fix. One line, plus `import array`:
      `elif isinstance(src_value, (list, tuple, array.array)):`. After applying it, every
      row of the table above round-trips as expected."
    - "Not fixable from the ROS side. Reassigning `msg.position = list(...)` before sending
      does not help, because the rclpy setter converts it back to array.array. The change
      has to be inside type_mapper."
    - "Confirmed end to end across an OS boundary after the patch. With the patched bridge
      on WSL and an independent Windows peer decoding the received PDUs, two consecutive
      runs delivered 1222 and 1185 JointState PDUs, every one with a non-empty position
      array, values matching the sender's known sin/cos waveform, and effort holding its
      constant [0.1, 0.2, 0.3, 0.4]."

sources:
  repositories:
    - repository: hakoniwalab/hakoniwa-pdu-ros
      path: hakoniwa_pdu_ros/type_mapper.py
      revision: 8693f4d82a61765feb990754459427a287347bb3
    - repository: hakoniwalab/hakoniwa-pdu-ros
      path: test/test_type_mapper.py
      revision: 8693f4d82a61765feb990754459427a287347bb3
  runtime_evidence:
    - description: >-
        External contributor's WSL2 Ubuntu 24.04 / ROS 2 Jazzy environment plus a native
        Windows 11 x86_64 peer, 2026-07-30/31. Before the patch, a real rclpy JointState
        arrived with name populated and position/velocity/effort empty. A field-by-field
        probe covered JointState.position, LaserScan.ranges and Float64MultiArray.data.
        After the patch, the same bridge delivered 1222 and 1185 PDUs across two runs with
        all numeric arrays populated and their element counts equal to the joint-name count.
  expert_input: []

confidence: high

contradictions: []

validation:
  status: verified
  required:
    - Decide whether numpy.ndarray should be accepted in the same branch. rclpy can be
      configured to return numpy arrays, and that representation is neither list, tuple nor
      array.array, so it would fail the same way. Not measured here.
    - Confirm whether upstream wants a warning when an unrecognized sequence type reaches
      the final else branch. Silent skipping is what turned a data-loss bug into a
      partial-success illusion.
    - Add at least one test that uses real rclpy message types. The fake-class suite is
      fast but structurally incapable of catching this class of defect.
  notes:
    - Verified on one machine, one RMW implementation, one Jazzy release, with three field
      types probed directly plus an end-to-end cross-OS confirmation after the fix.

tracking:
  implementation_status: none
  issues: []

promotion_targets:
  catalog: []
  runtime_primer: false
  base_ecosystem_guide: false
  component_asset_guide: false
  recipes: []
  executable_guardrails:
    - repository: hakoniwalab/hakoniwa-pdu-ros
      kind: test
      path: test/test_type_mapper.py
      mode: detect-only
      notes:
        - A test built on fake message classes cannot observe the representation real rclpy
          actually uses. One real-rclpy round trip per verified type would have failed
          before this patch and would keep failing if the branch regresses.

promotion_rationale:
  - The defect silently destroys the numeric content of every field it was measured on, and
    those fields belong to three of the five message types the README advertises as
    verified, so the correction belongs in the owning repository rather than in a Recipe or
    a catalog note.
  - The guardrail is proposed against the test suite because the one-line source fix is
    trivial while the reason it went unnoticed is structural.

resolution:
  status: pending
  fixed_by: []
  verified: false
  promoted_to: []
  executable_guardrails: []
  notes:
    - Diagnosis, patch and end-to-end confirmation only; no upstream issue filed, since
      issue filing for implementation work is owned by the upstream maintainers.
    - The patch lives in the external contributor's workspace as
      0001-type_mapper-accept-array-array.patch and is applied to a clone before install,
      so the vendored checkout stays restorable with git apply.
